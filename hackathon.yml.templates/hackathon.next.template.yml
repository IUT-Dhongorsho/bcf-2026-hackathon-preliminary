version: 1

backend:
  working_dir: backend

  env:
    example_file: .env.example
    output_file: .env

  install:
    - npm install

  run:
    - npm run build
    - npm start

server:
  base_url: http://localhost:3000
  health_endpoint: /health

timeouts:
  startup: 90
  request: 30